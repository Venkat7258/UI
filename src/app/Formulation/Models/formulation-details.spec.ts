import { FormulationDetails } from './formulation-details';

describe('FormulationDetails', () => {
  it('should create an instance', () => {
    expect(new FormulationDetails()).toBeTruthy();
  });
});
