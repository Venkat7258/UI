import { RequiredValidatorLogicDirective } from './required-validator-logic.directive';

describe('RequiredValidatorLogicDirective', () => {
  it('should create an instance', () => {
    const directive = new RequiredValidatorLogicDirective();
    expect(directive).toBeTruthy();
  });
});
