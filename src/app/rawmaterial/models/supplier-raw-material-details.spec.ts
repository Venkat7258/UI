import { SupplierRawMaterialDetails } from './supplier-raw-material-details';

describe('SupplierRawMaterialDetails', () => {
  it('should create an instance', () => {
    expect(new SupplierRawMaterialDetails()).toBeTruthy();
  });
});
