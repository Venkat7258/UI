import { SupplierRawMaterialSubComponentDetails } from './supplierRawMaterialSubComponentDetails';

describe('Regulations', () => {
  it('should create an instance', () => {
    expect(new SupplierRawMaterialSubComponentDetails()).toBeTruthy();
  });
});
