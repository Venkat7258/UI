import { SubComponentFunctions } from './sub-component-functions';

describe('SubComponentFunctions', () => {
  it('should create an instance', () => {
    expect(new SubComponentFunctions()).toBeTruthy();
  });
});
