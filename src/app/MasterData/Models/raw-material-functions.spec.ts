import { RawMaterialFunctions } from './raw-material-functions';

describe('RawMaterialFunctions', () => {
  it('should create an instance', () => {
    expect(new RawMaterialFunctions()).toBeTruthy();
  });
});
