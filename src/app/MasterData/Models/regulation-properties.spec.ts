import { RegulationProperties } from './regulation-properties';

describe('RegulationProperties', () => {
  it('should create an instance', () => {
    expect(new RegulationProperties()).toBeTruthy();
  });
});
